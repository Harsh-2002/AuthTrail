# AuthTrail sensitive-command classification (section 20) - one substring pattern per line, simple matching, not threat detection.
# Only controls the optional Slack alert (AUTH_TRAIL_SLACK_COMMAND_ALERTS=1, default 0); every command is always logged locally regardless.

useradd
userdel
usermod
passwd
visudo
sudo
su
systemctl stop
systemctl restart
reboot
shutdown
poweroff
iptables
nft
ufw
docker run
docker exec
docker rm
chmod /etc/
chown /etc/
sshd_config
sudoers
authorized_keys
