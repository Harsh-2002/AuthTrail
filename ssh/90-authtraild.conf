# AuthTrail SSH configuration drop-in (section 8) - install.sh always runs sshd -t before reloading with this present, never applied blindly.
# LogLevel VERBOSE logs the auth key fingerprint (needed for keys.map); ExposeAuthInfo is left on per spec but not currently parsed.

LogLevel VERBOSE
ExposeAuthInfo yes
